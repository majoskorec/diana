{% extends 'OroUIBundle:actions:index.html.twig' %}
{% import 'OroUIBundle::macros.html.twig' as UI %}
{% set gridName = 'locality-grid' %}
{% set pageTitle = 'spk.huntingground.locality.entity_plural_label'|trans %}

{% block navButtons %}
    {#{% include 'OroImportExportBundle:ImportExport:buttons.html.twig' with {
        entity_class: entity_class,
        exportProcessor: 'orocrm_account',
        exportTemplateProcessor: 'orocrm_account',
        importProcessor: 'orocrm_account.add_or_replace',
        importTitle: 'oro.importexport.import.entity'|trans({'%entityName%': 'orocrm.account.entity_plural_label'|trans}),
        dataGridName: gridName
    } %}#}

    {% if resource_granted('spk_huntingground_locality_create') %}
        <div class="btn-group">
            {{ UI.addButton({
                'path' : path('spk_huntingground_locality_create'),
                'entity_label': 'spk.huntingground.locality.entity_label'|trans
            }) }}
        </div>
    {% endif %}
{% endblock %}